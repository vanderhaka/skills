interface:
  display_name: "Bug Ripple"
  short_description: "Diagnose one bug and sweep for sibling failures"
  default_prompt: "Use $bug-ripple to diagnose this bug with a tight feedback loop, define the blast radius, and search for sibling failures without broadening into a repo-wide review."
