interface:
  display_name: "Feature Graph Plan"
  short_description: "Create the orchestration dependency graph"
  default_prompt: "Use $feature-graph-plan to create the canonical feature dependency graph and progress file."
