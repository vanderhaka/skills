interface:
  display_name: "Safe Feature Slice"
  short_description: "Plan and execute safe feature slices"
  default_prompt: "Use $safe-feature-slice to plan, build, review, or continue safe feature work; create a slice plan first when scope is broad, then execute eligible slices with invariant and behaviour-preservation checks."
