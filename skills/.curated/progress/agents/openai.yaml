interface:
  display_name: "Progress"
  short_description: "Strict production progress protocol"
  default_prompt: "Use progress to discover, coordinate active worktrees, approve, execute, QA, and package formal plus plain-English summaries."
